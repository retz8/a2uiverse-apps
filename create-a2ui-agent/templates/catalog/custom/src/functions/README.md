# functions/

One file per client-side function the catalog declares, such as a validation, a formatter or a local effect, built with `createFunctionImplementation` from `@a2ui/web_core/v0_9`. Register each in `FUNCTIONS` in `../catalog.registry.ts` and in `../catalog.ts`.

Empty until the catalog needs one.
