# functions/

One module per local function the catalog declares (`and.ts`, `formatDate.ts`, ...), built with
`createFunctionImplementation` from `@a2ui/web_core/v0_9` and registered in
`../catalog.registry.ts` (`FUNCTIONS`) and `../catalog.ts`. Empty until a surface needs one.
