# __DISPLAY_NAME__ domain knowledge

What the objects in this domain are, how they relate, and what a person is deciding when they ask
about one. This doc holds **facts and the decisions that hinge on them** — never what a screen
should contain. Composition is the model's: it reads the request, works out what the person is
trying to decide, and builds the surface that serves it.

Register: declarative. `brand-guidance.md` is imperative and covers how to build in the product's
visual language; this covers what the agent is building _about_. A fact the model would already
apply earns no place here — what follows is what is easy to get wrong, or what a decision
genuinely turns on.

---

## The objects

<!-- TODO: the unit of the domain (a thread, an issue, an event), what identifies one, how they
relate, and which field is authoritative for what. -->

- The domain's primary object is a placeholder until this doc is written.

## What a request is deciding

<!-- TODO: for each kind of request the card advertises, what the person is actually trying to
decide, and which fetched fields that decision turns on. -->

- A greeting request decides nothing; it is the scaffold's placeholder.
